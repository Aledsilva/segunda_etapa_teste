import 'package:flutter/material.dart';

class BookPresentantion extends StatelessWidget {
  const BookPresentantion({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
